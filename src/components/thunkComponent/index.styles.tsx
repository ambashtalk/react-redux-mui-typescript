import React from "react";
import { Button, Container, css } from "@mui/material";
import styled from "@emotion/styled";

export const ThunkContainer = styled(Container)(css`
  height: 50vh;
`);

export const FetchButton = styled(Button)(css``);
